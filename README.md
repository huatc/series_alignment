# series_alignment
A series alignment algorithm to match molecular simulations with experimental characterization. 

<p align="center">
  <img src="Figures/TOC.png" width="550" height="350">
</p>


## Installation (for conda)
To install the package, first git clone the repository. Then install the dependencies using:

```
conda env create -f environment.yml -n series_distance
```